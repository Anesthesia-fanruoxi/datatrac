# 自动模式实现

## 📋 文档说明

本文档详细描述自动模式的执行流程和实现细节。

**文档版本**: v1.0  
**最后更新**: 2026-01-30  
**实现状态**: ✅ 已完成

---

## 🎯 概述

自动模式是 DataTrac 的默认且唯一的调度模式，任务启动后自动调度所有未完成的任务单元并发执行。

**核心特性**:
- 自动加载所有未完成单元
- 并发执行（Semaphore 控制）
- 实时状态同步
- 支持断点续传

---

## 🔄 完整执行流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 用户启动任务                                              │
│    └─ 前端调用 start_sync_task(taskId)                      │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. SyncEngine::start_sync                                    │
│    ├─ 加载任务配置 (TaskLoader::load_and_parse_task)        │
│    ├─ 初始化任务单元 (TaskLoader::init_task_units)          │
│    └─ 调用 TaskManager::execute_auto_mode                   │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. TaskManager::execute_auto_mode                            │
│    ├─ 从数据库加载任务单元                                   │
│    ├─ 过滤未完成单元 (status != completed)                  │
│    ├─ 创建 Semaphore (concurrency)                          │
│    └─ 为每个单元创建异步任务                                 │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 并发执行任务单元                                          │
│    ├─ 获取 Semaphore 许可                                   │
│    ├─ 标记为 Running                                        │
│    ├─ 调用 process_fn 执行同步                              │
│    ├─ 成功 → Completed / 失败 → Failed                      │
│    └─ 释放 Semaphore 许可                                   │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 状态同步                                                  │
│    ├─ 内存缓存更新                                           │
│    ├─ 数据库持久化                                           │
│    ├─ ProgressMonitor 聚合                                  │
│    └─ 前端实时更新                                           │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 等待所有任务完成                                          │
│    └─ 返回成功执行的单元数量                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 详细实现步骤

### 步骤 1: 加载任务配置

```rust
// SyncEngine::start_sync
pub async fn start_sync(&self, task_id: &str) -> Result<()> {
    log::info!("开始同步任务: {}", task_id);
    
    // 加载任务配置
    let config = self.task_loader.load_and_parse_task(task_id).await?;
    
    log::info!("任务配置加载完成: {}", config.task_name);
    log::info!("同步方向: {:?}", config.sync_direction);
    log::info!("线程数: {}", config.sync_config.thread_count);
    
    // ... 继续执行
}
```

### 步骤 2: 初始化任务单元

```rust
// 初始化任务单元（支持断点续传）
self.task_loader.init_task_units(task_id).await?;

log::info!("任务单元初始化完成");
```

**init_task_units 内部逻辑**:
1. 从 `task_unit_configs` 读取配置
2. 加载现有的 `task_unit_runtimes`
3. 对比配置和运行记录
4. 创建或更新运行记录
5. 清理孤立的运行记录

### 步骤 3: 执行自动模式

```rust
// 调用 TaskManager 执行自动模式
let success_count = self.task_manager.execute_auto_mode(
    task_id,
    config.sync_config.thread_count as usize,
    self.progress_monitor.clone(),
    |unit_id, unit_name| {
        let engine = self.clone();
        let config = config.clone();
        async move {
            // 执行具体的同步逻辑
            engine.sync_single_unit(&unit_name, &config).await
        }
    }
).await?;

log::info!("任务完成，成功: {}/{}", success_count, total_units);
```

### 步骤 4: 并发执行单元

```rust
// TaskManager::execute_auto_mode 内部
for unit in pending_units {
    // 获取 Semaphore 许可
    let permit = semaphore.clone().acquire_owned().await?;
    
    // 标记为运行中
    task_manager.update_unit_status_with_sync(
        &task_id,
        &unit.id,
        TaskUnitStatus::Running,
        &progress_monitor
    ).await?;
    
    // 异步执行任务
    tokio::spawn(async move {
        // 执行同步逻辑
        let result = process_fn(unit_id.clone(), unit_name.clone()).await;
        
        match result {
            Ok(_) => {
                // 标记为完成
                task_manager.update_unit_status_with_sync(
                    &task_id,
                    &unit.id,
                    TaskUnitStatus::Completed,
                    &progress_monitor
                ).await?;
            }
            Err(e) => {
                // 标记为失败
                task_manager.fail_unit_with_sync(
                    &task_id,
                    &unit.id,
                    e.to_string(),
                    &progress_monitor
                ).await?;
            }
        }
        
        // 释放许可
        drop(permit);
    });
}
```

---

## 🔧 同步单个单元

### ES→ES 示例

```rust
async fn sync_single_unit(&self, unit_name: &str, config: &SyncTaskConfig) -> Result<()> {
    log::info!("开始同步索引: {}", unit_name);
    
    // 1. 获取源和目标客户端
    let source_client = self.get_es_client(&config.source_id).await?;
    let target_client = self.get_es_client(&config.target_id).await?;
    
    // 2. 获取总记录数
    let total_count = source_client.count(unit_name).await?;
    log::info!("索引 {} 总记录数: {}", unit_name, total_count);
    
    // 3. 更新总记录数
    self.task_manager.update_unit_progress_with_sync(
        &config.task_id,
        unit_name,
        total_count,
        0,
        &self.progress_monitor
    ).await?;
    
    // 4. 批次同步
    let batch_size = config.sync_config.batch_size;
    let total_batches = (total_count + batch_size - 1) / batch_size;
    let mut processed = 0;
    
    for batch_num in 1..=total_batches {
        // 读取批次数据
        let records = source_client.fetch_batch(unit_name, batch_num, batch_size).await?;
        
        // 写入目标
        target_client.bulk_index(unit_name, &records).await?;
        
        // 更新进度
        processed += records.len() as u64;
        self.task_manager.update_unit_progress_with_sync(
            &config.task_id,
            unit_name,
            total_count,
            processed,
            &self.progress_monitor
        ).await?;
        
        // 添加日志
        self.progress_monitor.add_log(
            &config.task_id,
            LogLevel::Info,
            LogCategory::Realtime,
            format!("已同步 {} / {} 条记录 ({:.1}%)", 
                processed, total_count, 
                (processed as f64 / total_count as f64) * 100.0)
        );
    }
    
    // 5. 数据校验
    let target_count = target_client.count(unit_name).await?;
    if target_count == total_count {
        self.progress_monitor.add_log(
            &config.task_id,
            LogLevel::Info,
            LogCategory::Verify,
            format!("✓ 索引 {} 数据校验通过：源 {} 条 = 目标 {} 条", 
                unit_name, total_count, target_count)
        );
    } else {
        anyhow::bail!("数据校验失败：源 {} 条 != 目标 {} 条", total_count, target_count);
    }
    
    log::info!("索引 {} 同步完成", unit_name);
    Ok(())
}
```

---

## 📊 状态同步机制

### 三层同步

```
┌──────────────────────────────────────────────────────────┐
│ 第一层: 内存缓存                                          │
│  - TaskManager.task_units (HashMap)                      │
│  - 快速读写                                               │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│ 第二层: 数据库持久化                                      │
│  - task_unit_runtimes 表                                 │
│  - 断点续传支持                                           │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│ 第三层: 前端实时显示                                      │
│  - ProgressMonitor → Tauri Event                         │
│  - 用户界面更新                                           │
└──────────────────────────────────────────────────────────┘
```

### 同步代码

```rust
pub async fn update_unit_status_with_sync(
    &self,
    task_id: &str,
    unit_id: &str,
    status: TaskUnitStatus,
    progress_monitor: &Arc<ProgressMonitor>,
) -> Result<()> {
    // 1. 更新内存
    self.update_unit_status(task_id, unit_id, status.clone());
    
    // 2. 更新数据库
    if let Some(storage) = &self.storage {
        storage.update_runtime_status(task_id, unit_id, status).await?;
    }
    
    // 3. 推送到前端
    let units = self.get_task_units(task_id);
    progress_monitor.update_task_units(task_id, units);
    
    Ok(())
}
```

---

## 🎯 并发控制示例

### 场景: 10 个线程，100 个索引

```
初始状态:
- 待执行: 100 个索引
- 线程数: 10

执行过程:
T0: 启动 10 个异步任务（索引 1-10）
    - Semaphore 许可: 0/10
    - 运行中: 10
    - 等待中: 90

T1: 索引 1 完成
    - 释放 1 个许可
    - Semaphore 许可: 1/10
    - 索引 11 获取许可并开始
    - 运行中: 10
    - 等待中: 89

T2: 索引 2, 3 完成
    - 释放 2 个许可
    - 索引 12, 13 获取许可并开始
    - 运行中: 10
    - 等待中: 87

... 循环执行

T_final: 所有索引完成
    - Semaphore 许可: 10/10
    - 运行中: 0
    - 已完成: 100
```

---

## 💡 错误处理

### 单元失败不影响其他单元

```rust
// 在异步任务中捕获错误
tokio::spawn(async move {
    let result = process_fn(unit_id.clone(), unit_name.clone()).await;
    
    match result {
        Ok(_) => {
            // 成功，标记为 Completed
            task_manager.update_unit_status_with_sync(...).await?;
        }
        Err(e) => {
            // 失败，标记为 Failed，但不影响其他单元
            log::error!("单元 {} 执行失败: {}", unit_name, e);
            task_manager.fail_unit_with_sync(...).await?;
            // 继续执行其他单元
        }
    }
});
```

### 失败重试

```
用户查看失败单元
   ↓
调用 reset_failed_units API
   ↓
失败单元状态变为 Pending
   ↓
重新启动任务
   ↓
自动模式重新执行失败单元
```

---

## 🚀 性能优化

### 1. 并发执行
- 使用 Tokio 异步运行时
- Semaphore 控制并发数
- 充分利用 CPU 和网络资源

### 2. 批量操作
- 批量读取数据
- 批量写入数据
- 减少网络往返

### 3. 内存缓存
- 任务单元缓存在内存
- 避免频繁数据库查询
- 提高状态读取速度

### 4. 异步 I/O
- 数据库操作异步化
- 网络请求异步化
- 避免阻塞主线程

---

## 📚 相关文档

- [任务管理器](../features/任务管理器.md) - TaskManager 功能设计
- [任务调度与断点续传](../features/任务调度与断点续传.md) - 调度策略
- [任务管理器实现](./任务管理器实现.md) - TaskManager 实现细节
- [断点续传实现](./断点续传实现.md) - 断点续传实现细节

---

**文档维护**: DataTrac 开发团队  
**最后更新**: 2026-01-30
