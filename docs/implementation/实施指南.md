# DataTrac 系统实施指南

## 📋 文档说明

本文档是 DataTrac 系统的"施工图纸"，详细说明从零开始构建系统的步骤、顺序、文件创建和作用。

**文档版本**: v1.0  
**最后更新**: 2026-01-30  
**目标读者**: 开发人员

---

## 🎯 实施概览

### 建房子类比

```
地基（数据库）→ 框架（基础设施）→ 主体结构（核心模块）→ 装修（UI界面）
```

### DataTrac 实施顺序

```
阶段1: 数据库设计
   ↓
阶段2: 基础设施层
   ↓
阶段3: 数据访问层
   ↓
阶段4: 核心业务层
   ↓
阶段5: API 接口层
   ↓
阶段6: 前端实现
   ↓
阶段7: 集成测试
```

---

## 📦 阶段 1: 数据库设计（地基）

### 目标
建立数据存储的基础，定义所有表结构和关系。

### 实施步骤

#### 1.1 创建数据库初始化文件

**文件**: `src-tauri/src/storage/schema.sql`

**作用**: 定义所有表结构、索引和外键约束

**内容**:
```sql
-- 数据源表
CREATE TABLE data_sources (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    host TEXT NOT NULL,
    port INTEGER NOT NULL,
    username TEXT NOT NULL,
    password TEXT NOT NULL,
    database_name TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);

-- 任务主表
CREATE TABLE sync_tasks (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    source_id TEXT NOT NULL,
    target_id TEXT NOT NULL,
    source_type TEXT NOT NULL,
    target_type TEXT NOT NULL,
    config TEXT NOT NULL,
    status TEXT NOT NULL,
    sync_mode TEXT NOT NULL DEFAULT 'auto',
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    FOREIGN KEY (source_id) REFERENCES data_sources(id) ON DELETE CASCADE,
    FOREIGN KEY (target_id) REFERENCES data_sources(id) ON DELETE CASCADE
);

-- 任务单元配置表
CREATE TABLE task_unit_configs (
    id TEXT PRIMARY KEY,
    task_id TEXT NOT NULL,
    unit_name TEXT NOT NULL,
    unit_type TEXT NOT NULL,
    created_at INTEGER NOT NULL,
    FOREIGN KEY (task_id) REFERENCES sync_tasks(id) ON DELETE CASCADE,
    UNIQUE(task_id, unit_name)
);

-- 任务单元运行记录表
CREATE TABLE task_unit_runtimes (
    id TEXT PRIMARY KEY,
    task_id TEXT NOT NULL,
    unit_name TEXT NOT NULL,
    status TEXT NOT NULL,
    total_records INTEGER DEFAULT 0,
    processed_records INTEGER DEFAULT 0,
    error_message TEXT,
    started_at INTEGER,
    updated_at INTEGER NOT NULL,
    last_processed_batch INTEGER,
    FOREIGN KEY (task_id) REFERENCES sync_tasks(id) ON DELETE CASCADE,
    UNIQUE(task_id, unit_name)
);

-- 创建索引
CREATE INDEX idx_data_sources_type ON data_sources(type);
CREATE INDEX idx_sync_tasks_status ON sync_tasks(status);
CREATE INDEX idx_task_unit_configs_task_id ON task_unit_configs(task_id);
CREATE INDEX idx_task_unit_runtimes_task_id ON task_unit_runtimes(task_id);
CREATE INDEX idx_task_unit_runtimes_status ON task_unit_runtimes(status);
```

**依赖**: 无

**验证**: 运行 SQL 脚本，确保所有表创建成功



#### 1.2 创建数据模型文件

**文件**: `src-tauri/src/storage/models.rs`

**作用**: 定义 Rust 数据结构，对应数据库表

**内容结构**:
```rust
// 数据源类型枚举
pub enum DataSourceType { Mysql, Elasticsearch }

// 数据源模型
pub struct DataSource { ... }

// 任务状态枚举
pub enum TaskStatus { Idle, Running, Paused, Completed, Failed }

// 同步任务模型
pub struct SyncTask { ... }

// 任务单元配置模型
pub struct TaskUnitConfig { ... }

// 任务单元运行记录模型
pub struct TaskUnitRuntime { ... }

// 任务单元状态枚举
pub enum TaskUnitStatus { Pending, Running, Completed, Failed, Paused }
```

**依赖**: 
- `serde` (序列化)
- `sqlx` (数据库映射)

**验证**: 编译通过，类型定义正确

---

## 📦 阶段 2: 基础设施层（框架）

### 目标
建立系统的基础服务，包括数据库连接、加密、日志等。

### 实施步骤

#### 2.1 创建数据库连接池

**文件**: `src-tauri/src/storage/mod.rs`

**作用**: 管理 SQLite 连接池，提供数据库访问接口

**核心功能**:
- 初始化数据库连接池
- 执行 schema.sql 创建表
- 提供连接获取方法

**代码结构**:
```rust
pub struct Storage {
    pool: SqlitePool,
}

impl Storage {
    pub async fn new(db_path: &str) -> Result<Self> {
        // 创建连接池
        let pool = SqlitePool::connect(db_path).await?;
        
        // 执行 schema.sql
        sqlx::query(include_str!("schema.sql"))
            .execute(&pool)
            .await?;
        
        Ok(Self { pool })
    }
}
```

**依赖**: 
- `sqlx` (数据库)
- `tokio` (异步运行时)

**验证**: 启动应用，数据库文件创建成功

#### 2.2 创建加密服务

**文件**: `src-tauri/src/crypto.rs`

**作用**: 提供密码加密/解密功能（AES-256-GCM）

**核心功能**:
- `encrypt(plaintext: &str) -> Result<String>`
- `decrypt(ciphertext: &str) -> Result<String>`

**依赖**: 
- `aes-gcm` (加密算法)
- `base64` (编码)

**验证**: 单元测试加密解密往返

#### 2.3 创建日志系统

**文件**: `src-tauri/src/progress_logger.rs`

**作用**: 管理任务日志，支持分类

**核心功能**:
```rust
pub enum LogCategory { Realtime, Summary, Verify, Error }
pub enum LogLevel { Info, Warn, Error }

pub struct LogEntry {
    pub timestamp: String,
    pub level: LogLevel,
    pub category: LogCategory,
    pub message: String,
}

pub struct ProgressLogger {
    logs: Arc<RwLock<HashMap<String, Vec<LogEntry>>>>,
}

impl ProgressLogger {
    pub fn add_log(&self, task_id: &str, level: LogLevel, category: LogCategory, message: String);
    pub fn get_logs(&self, task_id: &str) -> Vec<LogEntry>;
}
```

**依赖**: 
- `serde` (序列化)
- `chrono` (时间戳)

**验证**: 添加日志并读取成功

#### 2.4 创建进度监控器

**文件**: `src-tauri/src/progress.rs`

**作用**: 聚合任务进度，推送到前端

**核心功能**:
```rust
pub struct ProgressMonitor {
    logger: Arc<ProgressLogger>,
    app_handle: AppHandle,
}

impl ProgressMonitor {
    pub fn update_task_units(&self, task_id: &str, units: Vec<TaskUnit>);
    pub fn add_log(&self, task_id: &str, level: LogLevel, category: LogCategory, message: String);
    pub fn emit_progress(&self, task_id: &str, progress: TaskProgress);
}
```

**依赖**: 
- `tauri` (事件推送)
- `ProgressLogger`

**验证**: 推送事件，前端能接收

---

## 📦 阶段 3: 数据交换框架 (底座核心)

### 目标
定义 Reader、Writer 和 Transformer 接口，建立通用的数据传输管道。

### 实施步骤

#### 3.1 定义标准数据模型
**文件**: `src-tauri/src/sync_engine/framework/mod.rs`
**作用**: 定义 `FieldValue` 和 `DataRecord`。

#### 3.2 定义 Reader/Writer Traits
**文件**: `src-tauri/src/sync_engine/framework/mod.rs`
**作用**: 定义 `SourceReader` 和 `TargetWriter` 异步接口。

#### 3.3 实现通用管道调度器
**文件**: `src-tauri/src/sync_engine/framework/pipeline.rs`
**作用**: 实现 `SyncPipeline`，协调 Reader -> Transformer -> Writer 的循环。

---

## 📦 阶段 4: 存储层与数据访问 (Persistence)

### 目标
封装数据库 CRUD 操作，提供类型安全的数据访问接口。

### 实施步骤

#### 3.1 数据源操作

**文件**: `src-tauri/src/storage/data_source_ops.rs`

**作用**: 数据源的增删改查

**核心方法**:
```rust
impl Storage {
    pub async fn save_data_source(&self, ds: &DataSource) -> Result<()>;
    pub async fn load_data_source(&self, id: &str) -> Result<Option<DataSource>>;
    pub async fn load_all_data_sources(&self) -> Result<Vec<DataSource>>;
    pub async fn update_data_source(&self, ds: &DataSource) -> Result<()>;
    pub async fn delete_data_source(&self, id: &str) -> Result<()>;
}
```

**依赖**: `Storage`, `DataSource`

**验证**: 单元测试 CRUD 操作

#### 3.2 任务操作

**文件**: `src-tauri/src/storage/sync_task_ops.rs`

**作用**: 同步任务的增删改查

**核心方法**:
```rust
impl Storage {
    pub async fn save_task(&self, task: &SyncTask) -> Result<()>;
    pub async fn load_task(&self, id: &str) -> Result<Option<SyncTask>>;
    pub async fn load_all_tasks(&self) -> Result<Vec<SyncTask>>;
    pub async fn update_task(&self, task: &SyncTask) -> Result<()>;
    pub async fn update_task_status(&self, id: &str, status: TaskStatus) -> Result<()>;
    pub async fn delete_task(&self, id: &str) -> Result<()>;
}
```

**依赖**: `Storage`, `SyncTask`

**验证**: 单元测试 CRUD 操作

#### 3.3 任务单元操作

**文件**: `src-tauri/src/storage/task_unit_ops.rs`

**作用**: 任务单元配置和运行记录的操作

**核心方法**:
```rust
impl Storage {
    // 配置表操作
    pub async fn save_unit_config(&self, config: &TaskUnitConfig) -> Result<()>;
    pub async fn load_unit_configs(&self, task_id: &str) -> Result<Vec<TaskUnitConfig>>;
    
    // 运行记录表操作
    pub async fn save_unit_runtime(&self, runtime: &TaskUnitRuntime) -> Result<()>;
    pub async fn load_unit_runtimes(&self, task_id: &str) -> Result<Vec<TaskUnitRuntime>>;
    pub async fn update_runtime_status(&self, task_id: &str, unit_name: &str, status: TaskUnitStatus) -> Result<()>;
    pub async fn update_runtime_progress(&self, task_id: &str, unit_name: &str, total: i64, processed: i64) -> Result<()>;
    pub async fn reset_runtime(&self, task_id: &str, unit_name: &str) -> Result<()>;
    pub async fn fail_runtime(&self, task_id: &str, unit_name: &str, error: &str) -> Result<()>;
    pub async fn cleanup_orphan_runtimes(&self, task_id: &str) -> Result<u64>;
}
```

**依赖**: `Storage`, `TaskUnitConfig`, `TaskUnitRuntime`

**验证**: 单元测试所有操作

---

## 📦 阶段 4: 核心业务层（主体结构）

### 目标
实现系统的核心业务逻辑，包括任务管理器、同步引擎等。

### 实施步骤

#### 4.1 创建任务管理器

**文件**: `src-tauri/src/task_manager.rs`

**作用**: 统一调度和管理所有同步任务

**核心结构**:
```rust
pub struct TaskManager {
    task_units: Arc<RwLock<HashMap<String, Vec<TaskUnit>>>>,
    storage: Option<Arc<Storage>>,
}

impl TaskManager {
    pub fn new() -> Self;
    pub fn with_storage(storage: Arc<Storage>) -> Self;
    
    // 任务单元管理
    pub async fn load_task_units_from_db(&self, task_id: &str) -> Result<Vec<TaskUnit>>;
    pub fn init_task_units(&self, task_id: &str, unit_names: Vec<String>);
    pub fn get_task_units(&self, task_id: &str) -> Vec<TaskUnit>;
    
    // 状态管理
    pub fn update_unit_status(&self, task_id: &str, unit_id: &str, status: TaskUnitStatus);
    pub async fn update_unit_status_with_sync(...) -> Result<()>;
    
    // 进度管理
    pub fn update_unit_progress(&self, task_id: &str, unit_id: &str, total: u64, processed: u64);
    pub async fn update_unit_progress_with_sync(...) -> Result<()>;
    
    // 错误处理
    pub fn fail_unit(&self, task_id: &str, unit_id: &str, error: String);
    pub async fn fail_unit_with_sync(...) -> Result<()>;
    
    // 自动模式执行
    pub async fn execute_auto_mode<F, Fut>(...) -> Result<usize>;
}
```

**依赖**: 
- `Storage`
- `ProgressMonitor`
- `tokio::sync::Semaphore`

**验证**: 单元测试各个方法

#### 4.2 创建任务加载器

**文件**: `src-tauri/src/sync_engine/task_loader.rs`

**作用**: 加载任务配置，初始化任务单元

**核心方法**:
```rust
pub struct TaskLoader {
    source_manager: Arc<DataSourceManager>,
}

impl TaskLoader {
    pub fn new(source_manager: Arc<DataSourceManager>) -> Self;
    
    // 加载并解析任务配置
    pub async fn load_and_parse_task(&self, task_id: &str) -> Result<SyncTaskConfig>;
    
    // 初始化任务单元（支持断点续传）
    pub async fn init_task_units(&self, task_id: &str) -> Result<()>;
}
```

**依赖**: 
- `Storage`
- `DataSourceManager`

**验证**: 测试加载配置和初始化单元

#### 4.3 创建状态管理器

**文件**: `src-tauri/src/sync_engine/state_manager.rs`

**作用**: 管理任务的运行状态（运行中、暂停等）

**核心方法**:
```rust
pub struct TaskStateManager {
    task_states: Arc<RwLock<HashMap<String, SyncTaskState>>>,
}

impl TaskStateManager {
    pub fn new() -> Self;
    pub fn get_task_state(&self, task_id: &str) -> Option<SyncTaskState>;
    pub fn set_task_state(&self, task_id: &str, state: SyncTaskState);
    pub fn set_running(&self, task_id: &str, is_running: bool);
    pub fn set_paused(&self, task_id: &str, is_paused: bool);
}
```

**依赖**: 无

**验证**: 测试状态管理



#### 4.4 创建数据源管理器

**文件**: `src-tauri/src/datasource.rs`

**作用**: 管理数据源连接，提供 MySQL 和 ES 客户端

**核心方法**:
```rust
pub struct DataSourceManager {
    storage: Arc<Storage>,
    crypto: Arc<CryptoService>,
}

impl DataSourceManager {
    pub fn new(storage: Arc<Storage>, crypto: Arc<CryptoService>) -> Self;
    
    // 获取数据源
    pub async fn get_data_source(&self, id: &str) -> Result<DataSource>;
    
    // 创建 MySQL 连接
    pub async fn create_mysql_pool(&self, ds: &DataSource) -> Result<MySqlPool>;
    
    // 创建 ES 客户端
    pub async fn create_es_client(&self, ds: &DataSource) -> Result<Elasticsearch>;
    
    // 测试连接
    pub async fn test_connection(&self, id: &str) -> Result<bool>;
}
```

**依赖**: 
- `Storage`
- `CryptoService`
- `sqlx` (MySQL)
- `elasticsearch` (ES)

**验证**: 测试连接各种数据源

#### 4.5 创建同步引擎主模块

**文件**: `src-tauri/src/sync_engine/mod.rs`

**作用**: 协调所有同步组件，提供统一的同步接口

**核心结构**:
```rust
pub struct SyncEngine {
    source_manager: Arc<DataSourceManager>,
    task_manager: TaskManager,
    task_loader: TaskLoader,
    state_manager: TaskStateManager,
    progress_monitor: Arc<ProgressMonitor>,
}

impl SyncEngine {
    pub fn new(...) -> Self;
    
    // 启动同步任务
    pub async fn start_sync(&self, task_id: &str) -> Result<()>;
    
    // 暂停任务
    pub async fn pause_sync(&self, task_id: &str) -> Result<()>;
    
    // 恢复任务
    pub async fn resume_sync(&self, task_id: &str) -> Result<()>;
    
    // 停止任务
    pub async fn stop_sync(&self, task_id: &str) -> Result<()>;
}
```

**依赖**: 
- `DataSourceManager`
- `TaskManager`
- `TaskLoader`
- `StateManager`
- `ProgressMonitor`

**验证**: 测试启动、暂停、恢复、停止

#### 4.6 创建同步实现（ES→ES）

**文件**: `src-tauri/src/sync_engine/implementations/es_to_es.rs`

**作用**: 实现 Elasticsearch 到 Elasticsearch 的同步逻辑

**核心方法**:
```rust
pub async fn sync_es_to_es(
    engine: &SyncEngine,
    config: &SyncTaskConfig,
) -> Result<()> {
    // 1. 获取源和目标客户端
    // 2. 使用 TaskManager 自动模式执行
    // 3. 为每个索引执行同步
    // 4. 批次读取和写入
    // 5. 更新进度
    // 6. 添加日志
    // 7. 数据校验
}

async fn sync_single_index(
    source_client: &Elasticsearch,
    target_client: &Elasticsearch,
    index_name: &str,
    config: &SyncTaskConfig,
    task_manager: &TaskManager,
    progress_monitor: &Arc<ProgressMonitor>,
) -> Result<()> {
    // 具体的单个索引同步逻辑
}
```

**依赖**: 
- `SyncEngine`
- `TaskManager`
- `ProgressMonitor`
- `elasticsearch`

**验证**: 测试 ES→ES 同步

#### 4.7 创建其他同步实现（框架）

**文件**: 
- `src-tauri/src/sync_engine/implementations/mysql_to_mysql.rs`
- `src-tauri/src/sync_engine/implementations/mysql_to_es.rs`
- `src-tauri/src/sync_engine/implementations/es_to_mysql.rs`

**作用**: 实现其他同步方向的逻辑（参考 ES→ES）

**实施顺序**: 
1. 先实现 ES→ES（已完成）
2. 再实现 MySQL→MySQL
3. 然后 MySQL→ES
4. 最后 ES→MySQL

**验证**: 每个实现单独测试

---

## 📦 阶段 5: API 接口层（对外接口）

### 目标
提供 Tauri Commands，让前端可以调用后端功能。

### 实施步骤

#### 5.1 创建 Commands 文件

**文件**: `src-tauri/src/commands.rs`

**作用**: 定义所有 Tauri Commands

**核心 Commands**:
```rust
// 数据源管理
#[tauri::command]
pub async fn list_data_sources() -> Result<Vec<DataSource>>;

#[tauri::command]
pub async fn create_data_source(data: CreateDataSourceRequest) -> Result<DataSource>;

#[tauri::command]
pub async fn update_data_source(id: String, data: UpdateDataSourceRequest) -> Result<DataSource>;

#[tauri::command]
pub async fn delete_data_source(id: String) -> Result<()>;

#[tauri::command]
pub async fn test_data_source_connection(id: String) -> Result<TestConnectionResponse>;

// 任务管理
#[tauri::command]
pub async fn list_sync_tasks() -> Result<Vec<SyncTask>>;

#[tauri::command]
pub async fn create_sync_task(data: CreateSyncTaskRequest) -> Result<SyncTask>;

#[tauri::command]
pub async fn update_sync_task(id: String, data: UpdateSyncTaskRequest) -> Result<SyncTask>;

#[tauri::command]
pub async fn delete_sync_task(id: String) -> Result<()>;

// 任务执行
#[tauri::command]
pub async fn start_sync_task(task_id: String) -> Result<()>;

#[tauri::command]
pub async fn pause_sync_task(task_id: String) -> Result<()>;

#[tauri::command]
pub async fn resume_sync_task(task_id: String) -> Result<()>;

#[tauri::command]
pub async fn stop_sync_task(task_id: String) -> Result<()>;

// 任务监控
#[tauri::command]
pub async fn get_task_units(task_id: String) -> Result<Vec<TaskUnit>>;

#[tauri::command]
pub async fn get_task_progress(task_id: String) -> Result<TaskProgress>;

#[tauri::command]
pub async fn reset_failed_units(task_id: String) -> Result<u64>;

// 数据源查询
#[tauri::command]
pub async fn get_databases(data_source_id: String) -> Result<Vec<String>>;

#[tauri::command]
pub async fn get_tables(data_source_id: String, database: String) -> Result<Vec<String>>;

#[tauri::command]
pub async fn get_indices(data_source_id: String) -> Result<Vec<String>>;
```

**依赖**: 
- `SyncEngine`
- `DataSourceManager`
- `Storage`

**验证**: 使用 Tauri 开发工具测试每个 Command

#### 5.2 注册 Commands

**文件**: `src-tauri/src/lib.rs` 或 `src-tauri/src/main.rs`

**作用**: 将 Commands 注册到 Tauri 应用

**代码**:
```rust
#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_shell::init())
        .invoke_handler(tauri::generate_handler![
            // 数据源管理
            commands::list_data_sources,
            commands::create_data_source,
            commands::update_data_source,
            commands::delete_data_source,
            commands::test_data_source_connection,
            // 任务管理
            commands::list_sync_tasks,
            commands::create_sync_task,
            commands::update_sync_task,
            commands::delete_sync_task,
            // 任务执行
            commands::start_sync_task,
            commands::pause_sync_task,
            commands::resume_sync_task,
            commands::stop_sync_task,
            // 任务监控
            commands::get_task_units,
            commands::get_task_progress,
            commands::reset_failed_units,
            // 数据源查询
            commands::get_databases,
            commands::get_tables,
            commands::get_indices,
        ])
        .setup(|app| {
            // 初始化全局状态
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

**验证**: 启动应用，确保所有 Commands 注册成功

---

## 📦 阶段 6: 前端实现（装修）

### 目标
实现用户界面，调用后端 API，展示数据和进度。

### 实施步骤

#### 6.1 创建类型定义

**文件**: `src/types/index.ts`

**作用**: 定义前端使用的 TypeScript 类型

**核心类型**:
```typescript
// 数据源类型
export type DataSourceType = 'mysql' | 'elasticsearch';
export interface DataSource { ... }

// 任务类型
export type TaskStatus = 'idle' | 'running' | 'paused' | 'completed' | 'failed';
export interface SyncTask { ... }
export interface SyncConfig { ... }

// 任务单元类型
export type TaskUnitStatus = 'pending' | 'running' | 'completed' | 'failed';
export interface TaskUnit { ... }

// 进度类型
export interface TaskProgress { ... }

// 日志类型
export type LogCategory = 'realtime' | 'summary' | 'verify' | 'error';
export interface LogEntry { ... }
```

**依赖**: 无

**验证**: TypeScript 编译通过

#### 6.2 创建 API 层

**文件**: 
- `src/api/datasource.ts`
- `src/api/task.ts`
- `src/api/monitor.ts`

**作用**: 封装 Tauri Command 调用

**示例** (`src/api/datasource.ts`):
```typescript
import { invoke } from '@tauri-apps/api/core';

export const datasourceApi = {
  async list(): Promise<DataSource[]> {
    return await invoke('list_data_sources');
  },
  
  async create(data: CreateDataSourceRequest): Promise<DataSource> {
    return await invoke('create_data_source', { data });
  },
  
  // ... 其他方法
};
```

**依赖**: `@tauri-apps/api`

**验证**: 调用 API 方法，确保能正确通信

#### 6.3 创建 Pinia Stores

**文件**: 
- `src/stores/datasource.ts`
- `src/stores/task.ts`
- `src/stores/monitor.ts`

**作用**: 管理应用状态，缓存数据

**示例** (`src/stores/datasource.ts`):
```typescript
import { defineStore } from 'pinia';
import { ref } from 'vue';
import { datasourceApi } from '../api/datasource';

export const useDataSourceStore = defineStore('datasource', () => {
  const dataSources = ref<DataSource[]>([]);
  const loading = ref(false);
  
  async function fetchDataSources() {
    loading.value = true;
    try {
      dataSources.value = await datasourceApi.list();
    } finally {
      loading.value = false;
    }
  }
  
  // ... 其他方法
  
  return {
    dataSources,
    loading,
    fetchDataSources,
    // ...
  };
});
```

**依赖**: 
- `pinia`
- `vue`
- API 层

**验证**: 测试 Store 方法

#### 6.4 创建页面组件

**文件**: 
- `src/views/DataSourceManagement.vue` - 数据源管理页面
- `src/views/SyncTaskConfig.vue` - 任务配置页面
- `src/views/TaskMonitor.vue` - 任务监控页面

**作用**: 实现用户界面

**实施顺序**:
1. 先实现数据源管理页面
2. 再实现任务配置页面（向导式）
3. 最后实现任务监控页面

**验证**: 手动测试每个页面功能

#### 6.5 创建业务组件

**文件**: 
- `src/components/SyncTaskWizard/` - 任务配置向导
- `src/views/TaskMonitor/components/` - 监控相关组件

**作用**: 可复用的业务组件

**核心组件**:
- `TaskList.vue` - 任务单元列表
- `ProgressPanel.vue` - 进度面板
- `LogPanel.vue` - 日志面板
- `TaskControl.vue` - 任务控制按钮

**验证**: 组件单独测试

#### 6.6 实现事件监听

**文件**: `src/views/TaskMonitor.vue` 或 `src/stores/monitor.ts`

**作用**: 监听后端推送的进度事件

**代码**:
```typescript
import { listen } from '@tauri-apps/api/event';

// 监听进度更新
const unlisten = await listen<TaskProgress>('task-progress', (event) => {
  const progress = event.payload;
  // 更新 UI
});

// 监听日志
const unlistenLog = await listen<LogEntry>('task-log', (event) => {
  const log = event.payload;
  // 添加到日志列表
});

// 组件卸载时取消监听
onUnmounted(() => {
  unlisten();
  unlistenLog();
});
```

**依赖**: `@tauri-apps/api`

**验证**: 启动任务，观察实时更新

---

## 📦 阶段 7: 集成测试（验收）

### 目标
端到端测试整个系统，确保所有功能正常工作。

### 测试清单

#### 7.1 数据源管理测试

- [ ] 创建 MySQL 数据源
- [ ] 创建 ES 数据源
- [ ] 测试连接
- [ ] 编辑数据源
- [ ] 删除数据源
- [ ] 密码加密存储验证

#### 7.2 任务配置测试

- [ ] 创建 ES→ES 任务
- [ ] 选择源和目标数据源
- [ ] 选择索引
- [ ] 配置同步参数
- [ ] 保存任务
- [ ] 编辑任务
- [ ] 删除任务

#### 7.3 任务执行测试

- [ ] 启动任务
- [ ] 观察任务单元初始化
- [ ] 观察并发执行
- [ ] 观察进度更新
- [ ] 暂停任务
- [ ] 恢复任务
- [ ] 停止任务

#### 7.4 断点续传测试

- [ ] 启动任务执行到一半
- [ ] 暂停任务
- [ ] 重新启动任务
- [ ] 验证已完成单元不重复执行
- [ ] 验证未完成单元继续执行

#### 7.5 错误处理测试

- [ ] 模拟网络错误
- [ ] 观察失败单元标记
- [ ] 重置失败单元
- [ ] 重新执行失败单元

#### 7.6 日志系统测试

- [ ] 查看实时日志
- [ ] 查看明细日志
- [ ] 查看校验日志
- [ ] 查看错误日志
- [ ] 验证日志分类正确

#### 7.7 性能测试

- [ ] 测试大量数据同步
- [ ] 测试高并发执行
- [ ] 测试内存占用
- [ ] 测试 CPU 占用

---

## 📊 文件创建顺序总结

### 后端文件创建顺序

```
1. src-tauri/src/storage/schema.sql
2. src-tauri/src/storage/models.rs
3. src-tauri/src/storage/mod.rs
4. src-tauri/src/crypto.rs
5. src-tauri/src/progress_logger.rs
6. src-tauri/src/progress.rs
7. src-tauri/src/storage/data_source_ops.rs
8. src-tauri/src/storage/sync_task_ops.rs
9. src-tauri/src/storage/task_unit_ops.rs
10. src-tauri/src/task_manager.rs
11. src-tauri/src/sync_engine/task_loader.rs
12. src-tauri/src/sync_engine/state_manager.rs
13. src-tauri/src/datasource.rs
14. src-tauri/src/sync_engine/mod.rs
15. src-tauri/src/sync_engine/implementations/es_to_es.rs
16. src-tauri/src/sync_engine/implementations/mysql_to_mysql.rs
17. src-tauri/src/sync_engine/implementations/mysql_to_es.rs
18. src-tauri/src/sync_engine/implementations/es_to_mysql.rs
19. src-tauri/src/commands.rs
20. src-tauri/src/lib.rs (注册 Commands)
```

### 前端文件创建顺序

```
1. src/types/index.ts
2. src/api/datasource.ts
3. src/api/task.ts
4. src/api/monitor.ts
5. src/stores/datasource.ts
6. src/stores/task.ts
7. src/stores/monitor.ts
8. src/views/DataSourceManagement.vue
9. src/views/SyncTaskConfig.vue
10. src/components/SyncTaskWizard/index.vue
11. src/components/SyncTaskWizard/Step1SelectDataSourceType.vue
12. src/components/SyncTaskWizard/Step2FilterIndices.vue
13. src/components/SyncTaskWizard/Step3SelectIndices.vue
14. src/components/SyncTaskWizard/Step4SyncConfig.vue
15. src/components/SyncTaskWizard/Step5Confirm.vue
16. src/views/TaskMonitor.vue
17. src/views/TaskMonitor/components/TaskList.vue
18. src/views/TaskMonitor/components/ProgressPanel.vue
19. src/views/TaskMonitor/components/LogPanel.vue
20. src/views/TaskMonitor/components/TaskControl.vue
```

---

## 🎯 关键依赖关系

### 后端依赖图

```
Storage (数据库)
   ↓
DataSourceManager, TaskManager, TaskLoader
   ↓
SyncEngine
   ↓
Commands
   ↓
前端 API 调用
```

### 前端依赖图

```
Types (类型定义)
   ↓
API Layer (API 封装)
   ↓
Stores (状态管理)
   ↓
Views & Components (UI 组件)
```

---

## 💡 实施建议

### 1. 增量开发
- 不要一次性创建所有文件
- 按阶段逐步实施
- 每个阶段完成后测试验证

### 2. 先后端后前端
- 先完成后端核心功能
- 使用 Tauri 开发工具测试 Commands
- 再实现前端界面

### 3. 先简单后复杂
- 先实现 ES→ES 同步（相对简单）
- 再实现其他同步方向
- 逐步完善功能

### 4. 持续测试
- 每个模块完成后立即测试
- 编写单元测试
- 进行集成测试

### 5. 文档同步
- 代码和文档同步更新
- 记录实施过程中的问题
- 更新设计文档

---

## 📚 相关文档

- [设计概览](../设计概览.md) - 系统整体设计
- [任务管理器](../features/任务管理器.md) - TaskManager 功能设计
- [任务调度与断点续传](../features/任务调度与断点续传.md) - 调度策略
- [数据库设计](../architecture/数据库设计.md) - 三表结构设计
- [任务管理器实现](./任务管理器实现.md) - TaskManager 实现细节
- [自动模式实现](./自动模式实现.md) - 自动模式执行流程
- [断点续传实现](./断点续传实现.md) - 断点续传实现细节

---

**文档维护**: DataTrac 开发团队  
**最后更新**: 2026-01-30
